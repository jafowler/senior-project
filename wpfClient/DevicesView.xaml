<UserControl x:Class="wpfClient.DevicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpfClient"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListView x:Name="DevicesViewList"  ItemsSource="{Binding mySystem}" HorizontalAlignment="Right" VerticalAlignment="Stretch" Width="331" Margin="0,23,0,0" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ListView ItemsSource="{Binding}" SelectedItem="{Binding SelectedSysInfo}">
                        <TextBlock Text="{Binding myDrives}"/>
                    </ListView>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</UserControl>
